pipeline {
    agent {
        docker { 
          image 'yatin03/amazonlinux:phpapache'
        }
    }
    stages {
        stage('Execute Script') {
            steps {
                sh 'ls -lahtr'
            }
        }
    }
}
