pipeline {
    agent {
        docker { 
          image 'yatin03/amazonlinux:phpapache'
        }
    }
    stages {
        stage('Execute Script') {
            steps {
                sh 'cd Source'
                sh '/bin/bash deployment/scripts/config_stage.sh'
            }
        }
    }
}
